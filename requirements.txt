discord.py>=2.3.2
python-dotenv>=1.0.0
aiohttp>=3.9.0
firebase-admin>=6.4.0
google-genai>=2.0.0
fastapi>=0.115.0
# Required by FastAPI for every multipart route — which is every craft, screenshot,
# flag and log upload in the API. It was installed only as a transitive dependency
# while DEPLOYMENT.md claimed it was pinned here; a resolver that stopped pulling it
# in would have taken out the whole upload surface at the next deploy.
python-multipart>=0.0.9
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
Pillow>=10.0.0
# QR codes for two-factor enrolment. Pure Python, no dependencies of its own;
# everything security-relevant (RFC 6238 itself) stays hand-rolled in data/twofa.py.
segno>=1.6.0
